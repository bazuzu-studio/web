# URL сайта — используется в metadata, sitemap.xml и robots.txt
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# URL backend API (Payload CMS). Пока не используется — все данные мокнуты
# в lib/data.ts. Когда backend (apps/cms по ТЗ) будет готов, раскомментировать
# и подключить в lib/api.ts.
# NEXT_PUBLIC_API_URL=http://localhost:3001
